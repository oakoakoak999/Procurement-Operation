# Secrets — never commit
.env
.gdrive-token.json
.gdrive-po-token.json
.gsheets-token.json

# Chrome debug profile (session-specific, not portable)
.chrome-debug-profile/

# Dependencies — reinstall with: npm install
node_modules/

# Playwright internals
.playwright/
.playwright-cli/

# Claude Code internals
.claude/

# Runtime data — not important to track
knowledge.json

# Drive sync scripts — belong in Obsidian vault tools, not this repo
download-from-drive.mjs
upload-logs.mjs
upload-vault-full.mjs
