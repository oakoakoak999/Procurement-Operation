# Odoo credentials
ODOO_USERNAME=your_odoo_username
ODOO_PASSWORD=your_odoo_password

# Google OAuth — same credentials for Drive + Sheets (tnp.phr@gmail.com)
GDRIVE_CLIENT_ID=your_client_id
GDRIVE_CLIENT_SECRET=your_client_secret

# GitHub personal access token — for GitHub REST API / gh CLI (repo + workflow scope)
# Used to list/trigger GitHub Actions and read run status locally. Never commit the real value.
GH_TOKEN=your_github_pat

# ── Cloudflare Access service token (gha cloud door) ────────────────────────
# ONLY needed to reach Odoo through the `gha-` hostname from an untrusted
# datacenter/cloud IP (e.g. GitHub-hosted runner). Leave BOTH unset on a
# Cloudflare-trusted inside-network machine — the scripts send no CF headers then.
# Get the pair from IT (Access → Service Auth). Sent as CF-Access-Client-Id /
# CF-Access-Client-Secret headers on every Odoo request.
#
# UAT-FIRST: the code reads this single unsuffixed pair. Put the UAT gha token
# here (in the GitHub secret for the runner; leave unset on a trusted-IP PC).
# The gha URL is NOT here — it goes in config.json / the CONFIG_JSON secret as
# `odooUrl` (URL is config, not env).
CF_ACCESS_CLIENT_ID=your_cf_access_client_id
CF_ACCESS_CLIENT_SECRET=your_cf_access_client_secret
#
# PRODUCTION (later — needs an env selector before these are read; NOT active yet):
#   CF_ACCESS_CLIENT_ID_PROD     = <prod client id>
#   CF_ACCESS_CLIENT_SECRET_PROD = <prod client secret>
#   CF_ACCESS_CLIENT_ID_UAT      = <uat client id>
#   CF_ACCESS_CLIENT_SECRET_UAT  = <uat client secret>
#   (each environment's gha URL still lives in its own CONFIG_JSON odooUrl)

# Machine-specific local paths
OBSIDIAN_VAULT_PATH=C:\Users\YourUsername\Desktop\Obsidian Vault
CLAUDE_MD_PATH=C:\Users\YourUsername\CLAUDE.md

# Google Drive folder IDs (machine-specific — check with Oak for correct values)
OBSIDIAN_BRAIN_FOLDER_ID=13FhD4f-ByWUyMx89HsVzzEuSW4be4x5n
CLAUDE_MD_FOLDER_ID=11de3g5uVMrQJwUeluNpTrPvpCxSOBdbv
OBSIDIAN_VAULT_FOLDER_ID=11de3g5uVMrQJwUeluNpTrPvpCxSOBdbv
